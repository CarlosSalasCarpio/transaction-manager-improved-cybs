{
  "name": "transaction-manager-improved-cybs",
  "version": "1.1.0",
  "private": true,
  "description": "Evolved CyberSource EBC2 Transaction Manager — Chrome extension + self-contained bookmarklet build.",
  "type": "module",
  "scripts": {
    "build:bookmarklet": "node build/build-bookmarklet.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
